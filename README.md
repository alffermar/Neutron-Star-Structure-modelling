# Neutron Star Structure Modelling

This project implements a dense nuclear matter model to study the equation of state (EoS) of a neutron star and solve the Tolman–Oppenheimer–Volkoff (TOV) equations to obtain mass–radius profiles.

The code is written in Fortran and is organized into modules to separate the main physical and numerical components:
- physical constants and model parameters,
- equation-of-state functions,
- numerical methods,
- nonlinear system solution,
- TOV integration.

## Problem description

The program solves a nonlinear system associated with a hadronic matter model including sigma, omega, and rho mesons, with possible contributions from pions and leptons. Starting from the baryon density, it computes:
- mesonic potentials,
- proton, neutron, and lepton densities,
- energy density and pressure,
- the equation of state stored in `EoS.dat`.

This EoS is then used to integrate the TOV equations and determine the stellar structure, including:
- radius,
- gravitational mass,
- radial pressure,
- density profile.

## Repository structure

- `precision.f95`: defines the numerical precision (`real128`).
- `Constants.f95`: physical constants, particle masses, and model parameters.
- `Funciones.f95`: definitions of densities, chemical potentials, mean field equations, energy density, pressure, and TOV derivatives.
- `NumMethods.f95`: numerical derivatives, interpolation, Jacobian construction, Brodyden's method system solver, and RK4 integration routines. Another auxiliar matrix methods such as Factorization LU system solver are included.
- `SistemaEcuaciones.f95`: solves the nonlinear system for different baryon densities fixed in `Constants.f95`.
- `TovsSolver.f95`: integrates the TOV equations using the generated EoS.
- `Bulk.f95`: computes quantities related to compressibility and effective mass behaviour.

## Requirements

- A Fortran compiler compatible with F2008 (for example, `gfortran`).
- A Linux/macOS/Windows development environment with Fortran support.
- Output files generated by the program (for example `EoS.dat`, `masas.dat`, etc.).

## Compilation

In order to compile the modules the auxiliar files must be compiled first and in order of nesting. Next, files with the extension .mod are created. Each time that one of the modules is edited, it must be recompiled with the `-c` flag uptdating the .mod file. It is recommended to recompile all modules at the same time when one of them is editted because they are nested.

```bash
gfortran -c precision.f95 Constants.f95 Funciones.f95 NumMethods.f95 
gfortran precision.f95 Constants.f95 Funciones.f95 NumMethods.f95 SistemaEcuaciones.f95 -o SistemaEcuaciones.exe
```
To execute the programs `TovsSolver.f95` and `Bulk.f95` substitute `SistemaEcuaciones.f95` and change the name of the program .exe.
## How to run

1. Compile `SistemaEcuaciones.f95`.
2. Run the generated binary to produce the matter-model output files.
3. Verify that `EoS.dat` has been generated.
4. Compile and run `TovsSolver.f95` and `Bulk.f95` using that equation of state.
5. Inspect the output in `Radio-Masa.dat` and the generated `.dat` files.

## Main outputs

- `EoS.dat`: energy density and pressure as a function of baryon density.
- `Preasure.dat`: pressure versus density.
- `Energydensity.dat`: energy density versus density.
- `momentos.dat`: meson field values.
- `poblaciones.dat`: proton, neutron, and lepton fractions.
- `Radio-Masa.dat`: stellar radius and mass for different TOV integration points.

## Model notes

- The baryon density is denoted as `nb` and is expressed in dimensionless units inside the model.
- The equation of state is constructed from the energies of fermions in the presence of mesonic fields.
- The TOV integration is performed using a fourth-order Runge–Kutta scheme.
- The code uses cubic interpolation and iterative methods to solve the nonlinear equations.

## Important notes

- Some variable and comment names are in Spanish. If the project is to be maintained or shared more broadly, it is recommended to improve naming consistency and code readability.
- The implementation includes several experimental numerical routines and tests. Stability should be reviewed before using it for production scientific work.
- Some parts of the project are still under development; it is useful to document which variables correspond to each density, field, and equation.

## Conceptual background

This type of calculation is based on relativistic models of dense nuclear matter and the numerical solution of the TOV equations for stellar structure. The equations and numerical treatments are inspired by literature on:
- hadronic equations of state,
- neutron star modelling,
- meson fields in dense matter,
- numerical integration of differential equations.

## License

The repository does not currently specify a license. If the code is intended for public sharing, consider adding a license such as `MIT` or `GPL-3.0` depending on the intended use.

## Suggested next steps

- Add a section with example output plots or results.
- Document each function in `Funciones.f95` with brief scientific explanations.
- Add a flow diagram of the calculation pipeline.
- Standardize naming conventions across files and variables.
- Add numerical validation checks and basic tests.

## Project status

This project is currently under development and focuses on the physical and numerical modelling of neutron stars. The overall structure is already in place, but the documentation and code clarity can be improved substantially.
